steps:
- name: "gcr.io/cloud-builders/npm"
  args: ["install"]

- name: "gcr.io/cloud-builders/npm"
  args: ["run", "build"]

- name: "gcr.io/firebase-ci"
  args: ["deploy", "--project=carlosnycportfolio"]

options:
  logging: CLOUD_LOGGING_ONLY  # Ensure logs go to Google Cloud Logging